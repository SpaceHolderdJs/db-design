mermaid.initialize({
  theme: 'dark',
  themeVariables: {
    background: '#333333', // Dark background color
    primaryColor: '#ffffff', // Primary text color
    edgeLabelBackground: '#333333', // Background for edge labels
    tertiaryColor: '#444444', // Other color adjustments if needed
    // Add more theme variables as needed
  },
});
